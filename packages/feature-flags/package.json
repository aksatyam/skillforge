{
  "name": "@skillforge/feature-flags",
  "version": "0.1.0",
  "private": true,
  "description": "Per-org + per-user feature flag evaluation. Postgres source-of-truth, Redis read-through, in-process LRU. See ADR-013 §S7.T3.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@skillforge/db": "workspace:*",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
