{
  "name": "@skillforge/pii",
  "version": "0.1.0",
  "private": true,
  "description": "PII anonymization for outbound AI calls. See ADR-013 §Decision 2.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:recall": "vitest run test/recall.spec.ts",
    "build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
