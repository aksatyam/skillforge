{
  "name": "@skillforge/tenant-guard",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@skillforge/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
